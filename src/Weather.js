import React from "react";

import "./Weather.css"

export default function Weather() {
    return (
        <p className="bajskorv">Search City in the search field above to get a Weather Forecast</p>
        );
}